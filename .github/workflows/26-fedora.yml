name: Deploy Image

on:
  push:
    branches: [ "26" ]
  schedule:
    - cron: '0 0 */14 * *'

permissions:
  contents: read
  packages: write

jobs:
  call-docker-publish:
    uses: ./.github/workflows/reusable.yml
    with:
      dockerfile: fedora/Containerfile